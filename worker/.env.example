# Postgres — same database the API writes submissions to
DATABASE_URL=postgresql://postgres:password@localhost:5432/octree

# Redis — backs the BullMQ submission queue (must match the API)
REDIS_URL=redis://localhost:6379

# Judge0 (self-hosted code execution)
JUDGE0_URL=http://94.130.34.33:2358
# JUDGE0_AUTH_TOKEN=          # only if the instance has authn enabled

# Worker tuning (optional)
# WORKER_CONCURRENCY=5
# JUDGE0_POLL_INTERVAL_MS=1000
# JUDGE0_MAX_POLL_ATTEMPTS=30
