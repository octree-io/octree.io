{
  "name": "@octree/mock-judge0",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A dependency-free mock of the Judge0 CE API for local dev (Judge0 runs poorly on macOS). Returns dummy results instead of executing code.",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
